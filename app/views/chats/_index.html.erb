<div class="mb-3">
  <table class="rooms">
   <tbody>
    <tr>
        <% @chat_partners.each do |chat_partner|  %>
          <%=link_to "ChatRoom #{chat_partner.nickname}さんへ","/chat/#{chat_partner.id}" %> </br>
        <%end%>
    </tr>
   </tbody>
  </table>
</div>

<p class="card-text"></p>